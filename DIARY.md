# 건마에반하다 활동 일지

> 새 활동과 검증 결과를 최신순으로 기록한다. 비밀값·로그인 정보는 기록하지 않는다.

## 2026-08-17 KST — 하트 로고·gban.kr 공개 전환

- `건마에반하다` 이름에 맞춰 분홍색 리본형 하트 심볼을 새로 제작하고 투명 배경 원본과 32·192·512px 파생 아이콘을 고정 자산으로 등록했다. 상단 페이지바와 하단 브랜드명 왼쪽에는 장식 타일 없이 심볼 자체만 배치했다.
- 로고 원본의 RGBA 알파 채널과 파생 아이콘 치수를 회귀 테스트로 고정하고, 브라우저에서 상단 왼쪽 심볼·한글 워드마크의 정렬과 투명 배경을 직접 확인했다.
- 운영 도메인을 `https://gban.kr`로 확정해 canonical, Open Graph URL, sitemap, robots.txt와 전체 페이지의 `index, follow` 설정을 한 번에 전환했다. `www`는 apex 도메인으로 통일한다.
- `pnpm verify` 결과 Vitest 29/29, 타입 검사, ESLint, 1,303개 정적 파일 생성과 빌드 감사가 모두 통과했다. 감사 수치는 공개 페이지 1,299, 지역 페이지 1,291, sitemap URL 1,299, 지역 원본 130, 지역 WebP 390이다.

## 2026-08-17 KST — GitHub 초기 게시

- 기존 운영 플랫폼과 같은 규칙으로 `dearmykassy/geonmae-banhada` 비공개 저장소를 만들고 `main` 브랜치에 전체 구현을 초기 게시했다.
- 배포에 필요한 소스, 잠금 파일, Netlify 설정, 390개 지역 WebP와 비지역 활성 이미지는 포함했다. `node_modules`, `.next`, `out`, TypeScript 캐시, 환경값, 지역 원본 PNG, 생성 캠페인·접촉시트·검수 영수증 아카이브는 제외했다.
- 게시 전 대상 611개·20,595,676바이트를 감사했다. 최대 파일은 692,454바이트였고 50MB 초과 파일, 심볼릭 링크, 비밀키·토큰 후보는 없었다.
- `pnpm verify`로 Vitest 26/26, 타입 검사, ESLint, 1,303개 정적 파일 생성과 빌드 감사를 다시 통과했다. 실제 도메인이 없으므로 preview `.invalid`, noindex/nofollow, robots 차단은 유지했다.

## 2026-08-17 KST — 모바일 홈 히어로 인물 초점 조정

- 680px 이하 홈 히어로에서 사진 레이어의 가로 초점을 `80%`로 옮겨, 오른쪽에 치우쳐 보이던 여성을 화면 중앙선보다 살짝 오른쪽에 배치했다. 오버레이와 fallback 배경 레이어의 위치는 유지했다.
- `pnpm build`로 1,303개 정적 페이지 재생성과 CSS 컴파일을 확인했고, 배포용 `out`에 모바일 전용 다중 배경 위치 규칙이 포함된 것을 확인했다.

## 2026-08-17 KST — Template4 구현·지역 이미지 릴리스·최종 검증

- Template4의 안내 바, 투명 헤더, 전체 화면 지역 검색, 히어로, 지역 카드, 운영 공지, 가격표, 이용 절차, 블로그, 연락 배너 구조를 `건마에반하다` 전용 화면으로 구현했다.
- 마사지봄과 byte-identical인 지역 원장 2개를 사용해 11개 시작 권역, 127개 허브, 1,153개 대표 지역의 정확한 1,291개 정적 지역 경로를 구성했다. 검색창은 동·읍·면과 지역 별칭까지 조회한다.
- 홈·고정 6페이지·블로그 2편·지역 1,291페이지에 title, keywords, description을 적용했다. 지역 키워드는 지시된 8개 계열이며 총 10,328개가 고유하다.
- 지역 본문의 잘못된 순환 계수를 수정하고 경로 해시 기반 문장 선택과 실제 하위 지역·상위 지역·행정동 정보를 반영했다. 지역명·브랜드 정규화 뒤에도 전체 1,291개 본문 조합이 모두 고유하며 메타 설명은 1,025종, 두 번째 훅은 1,038종이다. 필링홈타이·랑테라피와 고객 마케팅 문장 중복은 0건이다.
- Template4 전용 지역 거울 셀피 130개를 승인하고 desktop 1600×900, tablet 1200×675, mobile 768×600 WebP 390개로 릴리스했다. 1,291개 경로 배정은 `121×10 + 9×9`, 최대 재사용 10회, 부모·자식 충돌 0, 형제 충돌 0이다.
- 중앙 crop에서 인물이나 휴대폰이 잘리던 19개는 원본을 재생성하지 않고 자산별 focal 좌표를 적용했다. full source와 390×620·320×620 실제 노출 접촉시트에서 얼굴·휴대폰·거울을 전수 확인했다.
- 홈·기능·카테고리·블로그·연락 배너용 사진 17개와 투명 브랜드 마크 1개도 활성 경로에 연결했다. 최초 feature-04 거절본은 이력으로 보존하고 검수 통과한 v2만 사용한다.
- `pnpm verify` 결과 Vitest 26/26, 타입 검사, ESLint, 1,303개 정적 파일 생성, 빌드 감사가 모두 통과했다. 감사 수치는 공개 페이지 1,299, 지역 페이지 1,291, sitemap URL 1,299, 지역 원본 130, 지역 WebP 390이다.
- 브라우저에서 1440px 홈, 390px 지역 배너, 320px 홈·지역·가격·이용·공지·블로그 8개 경로를 확인했다. 가로 overflow와 깨진 이미지가 없고, `역삼동` 검색 결과가 정확한 상세 경로로 이동한다.
- 운영 현황 엑셀에 여섯 번째 플랫폼으로 추가했다. 실제 도메인은 아직 없으므로 preview `.invalid`, noindex/nofollow, robots 차단 상태를 유지한다.

## 2026-08-17 KST — 고객 문구 용어·독립성 교정

- 이용 시간을 뜻하는 문맥에 잘못 쓰인 `분수`를 고객 화면과 블로그·공지·가격·이용 안내·지역 문장 전반에서 `이용 시간`, `분 단위`, `시간`으로 바로잡았다.
- 필링홈타이와 겹쳤던 지역 H1 문형 2개를 `받을 주소와 희망 일정`, `건마에반하다 공개 금액`으로 교체하고, 말단 지역 안내 요약도 `상세 주소를 통화에서 확인하는 지역`으로 다시 작성했다.
- 지역명과 브랜드명을 치환한 뒤에도 기존 필링홈타이 문형 3개와 일치하지 않는 정규화 회귀 테스트, 고객 문구 파일에 `분수`가 남지 않는 회귀 테스트를 추가했다.
- 현행 필링홈타이 지역 문장과 다시 비교한 결과 12자 이상 고객 문구의 정규화 의미 중복은 0개, TypeScript 문자열 리터럴 완전 중복은 0개였다. 필수 지역 키워드 계열은 비교에서 제외하고 그대로 유지했다.
- `pnpm exec vitest run tests/regions-content.test.ts` 1개 파일 10개 테스트, `pnpm typecheck`, `pnpm lint`가 모두 통과했다.

## 2026-08-17 KST — Template4 거울 셀피 이미지 캠페인 스캐폴딩

- `geonma-template4-mirror-selfie-v1` 캠페인에 지역 원본 130개를 A~J 10개 lane에 13개씩 고정했다. A~D는 밝은 사진 52개, E~J는 은은하게 어두운 사진 78개다.
- 각 지역 사진은 최대 10개 경로에서만 재사용하며, 활성 1,291개 경로에는 121개 원본을 10회, 9개 원본을 9회 배정하는 계약을 manifest에 기록했다.
- 실제 Template4 UI 슬롯을 다시 대조해 홈 히어로 1개, 기능 카드 8개, 카테고리 카드 5개, 블로그 2개 등 비지역 사진 16개와 투명 브랜드 마크 1개를 추가했다. 활성 이미지 슬롯이 없는 contact 사진은 만들지 않는다.
- 모든 사진 프롬프트에 성인 한국인 여성, 실제 거울 반사면 45% 이상, 거울 프레임 3면 이상 또는 전체 외곽선, 중앙 반응형 crop-safe 범위, 단정한 불투명 의상, 문자·로고·워터마크·복제 반사·왜곡 금지 조건을 고정했다.
- 생성물 147개는 모두 `NOT_GENERATED`, `PENDING`, `NOT_APPROVED`, `NOT_RELEASED` 상태다. 실제 이미지는 생성하거나 승인하지 않았다.
- `pnpm exec vitest run tests/create-template4-mirror-campaign.test.mjs` 검증 결과 1개 파일, 3개 테스트가 통과했다.
- 생성 스크립트를 두 번째 실행해 문서 295개가 모두 `exact`로 판정되고 덮어쓰기가 발생하지 않는 것을 확인했다. 테스트용 임시 파일의 드리프트는 `CAMPAIGN_DRIFT_REFUSED`로 차단된다.

## 2026-08-17 KST — Template4 신규 플랫폼 착수

- 브랜드 `건마에반하다`, 플랫폼 ID `geonmae-banhada`, 시각 정본 Template4를 확정했다.
- 마사지봄과 동일한 활성 1,291개 지역 그래프를 기준으로 신규 Next.js 정적 프로젝트를 구성하기 시작했다.
- 실제 도메인은 미정이므로 preview `.invalid`, noindex/nofollow, robots 전체 차단을 개발 기본값으로 둔다.
- 지역·홈 문구는 확인된 운영 사실만 사용하며 기존 플랫폼 문장을 복사하지 않는 전용 문장 체계로 재작성한다.
- 이미지 캠페인은 깨끗한 실내 거울이 명확히 보이는 성인 한국인 여성 거울 셀피 컨셉으로 별도 생성·검수한다.
